<div class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">

    <div class="navbar-brand"> <%= link_to 'Baseball Journey', user_path(current_user) %></div>

    <button class = "navbar-toggle" data-toggle="collapse" data-target=".navHeaderCollapse">
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>

    <div class="collapse navbar-collapse navHeaderCollapse">
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to 'Profile', user_path(current_user) %></li>
        <li class = "dropdown" >
          <a href = "#" class="dropdown-toggle" data-toggle="dropdown">Stadiums <b class="caret"></b></a>
          <ul class="dropdown-menu">
            <% Stadium.all.each do |s| %>
            <li><%= link_to s.name, stadium_path(s) %></li>
            <% end %>
          </ul>
        </li>
        <li class = "dropdown" >
          <a href = "#" class="dropdown-toggle" data-toggle="dropdown"> Entries <b class="caret"></b></a>
          <ul class="dropdown-menu">
            <% current_user.entries.each do |e| %>
            <li><%= link_to e.title, entry_path(e) %></li>
            <% end %>
          </ul>
        </li>
        <li> <%= link_to 'Logout', logout_path %></li>
      </ul>
    </div>
  </div>
</div>
